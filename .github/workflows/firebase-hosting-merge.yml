# This file was auto-generated by the Firebase CLI and modified by jmischler72
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build-only
        env:
          VITE_TENOR_API_KEY: ${{ secrets.VITE_TENOR_API_KEY }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_SIMPLECHAT_7EA90 }}'
          channelId: live
          projectId: simplechat-7ea90
